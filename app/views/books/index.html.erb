<section class="hero">
  <div class="hero-content">
    <!-- Title and description of the website -->
    <h1>Welcome to Book Tracker</h1>
    <p>Track all the books you've read and keep your collection organized!</p>
    
    <!-- Link to the page where users can add a new book -->
    <a href="/books/new" class="cta-button">Add a New Book</a>
  </div>
</section>

<section class="book-listing">
  <div class="book-sections">
    
    <!-- Currently Reading Section -->
    <div class="book-section">
      <h2>Currently Reading</h2>
      <div class="books">
        <!-- Loop through the books that are currently being read -->
        <% @current_read.each do |book| %>
          <!-- Render the 'book_card' partial for each book in the current read list -->
          <%= render 'book_card', book: book %>
        <% end %>
      </div>
    </div>

    <!-- Completed Reads Section -->
    <div class="book-section">
      <h2>Completed Reads</h2>
      <h3>Total Read: <%= @finished_books.count %></h3>
      <div class="books">
        <!-- Loop through the books that are completed -->
        <% @finished_books.each do |book| %>
          <!-- Render the 'book_card' partial for each completed book -->
          <%= render 'book_card', book: book %>
        <% end %>
      </div>
    </div>
  </div>
</section>